# build_log

## Шаг 0 — Инициализация bootstrap-слоя проекта

Дата: 2026-04-04

### Что сделано
- Создан Django-каркас проекта с конфигурацией `config/` и окружениями `base/local/production`.
- Добавлены базовые приложения согласно архитектурной декомпозиции (`core`, `accounts`, `content`, `dialogs`, `analysis`, `platform_config`, `adminpanel`, `exports`, `auditlog`, `integrations`).
- Настроены базовые пути шаблонов, статики и медиа.
- Подключены Bootstrap 5 (CDN) и локальные `static/css/app.css`, `static/js/app.js`.
- Добавлен базовый шаблон `templates/layouts/base.html` и техническая страница `templates/pages/home.html`.
- Подготовлены `.env`-переменные для AI и email через `.env.example`.
- Добавлены `requirements.txt` и `.gitignore` для базовой разработки.

### Как это связано между собой
- `manage.py` запускает Django с настройками `config.settings.local`.
- `config/settings/base.py` задаёт общий каркас (apps, middleware, templates, db, static/media).
- `config/settings/local.py` и `config/settings/production.py` уточняют окружение через переменные среды.
- `config/urls.py` подключает `/admin/` и временную домашнюю страницу для smoke-проверки каркаса.
- Базовый шаблон и статические ресурсы создают фундамент UI для следующих итераций.

### Что не сделано в этом шаге
- Бизнес-логика доменных модулей и модели данных.
- Миграции, роли и права, чатовый runtime, аналитика и PDF.
- Реализация личного кабинета/энциклопедии и персонального таймера.

## Шаг 1 — База данных и core-модели

Дата: 2026-04-04

### Что сделано
- Добавлены базовые абстракции моделей в `apps/core/models.py`:
  - `TimestampedModel` (created_at/updated_at),
  - `ArchivableModel` (is_archived/archived_at),
  - `PublicIdModel` (public_id UUID).
- Добавлены ключевые enum-статусы в `apps/core/enums.py`:
  - `DialogStatus`,
  - `DialogEndedReason`,
  - `AnalysisRunStatus`.
- Введены базовые таблицы жизненного цикла:
  - `dialogs.DialogSession`,
  - `analysis.AnalysisRun`.
- Добавлены первые миграции:
  - `apps/dialogs/migrations/0001_initial.py`,
  - `apps/analysis/migrations/0001_initial.py`.

### Как это связано между собой
- `DialogSession` использует enum-статусы и фиксирует базовый жизненный цикл пользовательского диалога.
- `AnalysisRun` связан с `DialogSession` через `OneToOne` и хранит технический статус выполнения аналитики.
- Абстракции `TimestampedModel` и `PublicIdModel` обеспечивают единый формат технических полей для доменных моделей.
- Ограничение `uniq_active_dialog_per_user` поддерживает инвариант «один активный диалог на пользователя» на уровне БД.

### Что не сделано в этом шаге
- Полный состав полей `DialogSession`/`AnalysisRun` из спецификации (часть полей будет добавлена в следующих итерациях вместе с моделями контента и настроек).
- Модели `DialogMessage` и `AnalysisResult`.
- Доменные модели контента, платформенных настроек и ролей.

## Шаг 2 — Accounts, роли и auth flow

Дата: 2026-04-04

### Что сделано
- Реализована кастомная модель `accounts.User` с логином по email, ролями (`user/admin/superadmin`), признаком главного супер-админа и служебными полями профиля.
- Добавлен менеджер `UserManager` и функции генерации никнейма/цвета аватара.
- Реализованы формы:
  - регистрация,
  - вход по email,
  - обновление никнейма,
  - безопасный reset password.
- Реализованы views и маршруты auth-flow:
  - `register`, `login`, `logout`, `profile`,
  - `password_reset` + `done/confirm/complete`.
- Добавлен базовый throttling попыток логина через cache-сервис.
- Добавлен сервис ролевых permission-проверок (`is_admin`, `is_superadmin`, `is_primary_superadmin`, `can_create_admin`, `can_assign_superadmin`).
- Обновлены настройки проекта для кастомного пользователя (`AUTH_USER_MODEL`) и auth-redirects.
- Добавлена первая миграция `apps/accounts/migrations/0001_initial.py`.
- Добавлены smoke-тесты permission-сервиса ролей.

### Как это связано между собой
- `accounts.User` является источником истины для аутентификации и ролевого доступа.
- `LoginView` использует `EmailAuthenticationForm` и throttling-сервис из `accounts/services/auth.py`.
- Правила ролей из `accounts/services/permissions.py` готовы для интеграции в админские/контентные сервисы.
- `profile_view` даёт минимальный путь изменения никнейма без полноценного ЛК.

### Что не сделано в этом шаге
- Полноценный backoffice для управления ролями.
- Логирование событий превышения login-rate-limit в отдельную таблицу audit log.
- Seed-учётки и команды инициализации.

## Шаг 3 — Content domain

Дата: 2026-04-04

### Что сделано
- Реализованы модели контент-домена:
  - `Game`,
  - `Scenario`,
  - `ScenarioMediaAsset`,
  - `ScenarioPrompt`,
  - `AnalysisPrompt`,
  - `SystemPrompt`.
- Добавлены правила владения через абстракцию `OwnedModel` в `core`.
- Для контентных сущностей применены правила архивирования (`is_archived`, `archived_at`) и метки времени.
- Введены ограничения целостности:
  - уникальность slug/порядков,
  - один активный `ScenarioPrompt` на сценарий,
  - уникальность `AnalysisPrompt.alias` в рамках игры,
  - валидация диапазона `min_rating/max_rating`.
- Добавлена миграция `apps/content/migrations/0001_initial.py`.
- Добавлена management-команда `seed_demo_content` для заполнения demo-контента V1.
- Добавлены тесты ограничений контент-моделей.

### Как это связано между собой
- `Game` является корневой сущностью контента.
- `Scenario` принадлежит `Game`, может ссылаться на `ScenarioMediaAsset`.
- `ScenarioPrompt` принадлежит `Scenario` и хранит активную игровую версию промта.
- `AnalysisPrompt` принадлежит `Game` и задаёт критерии анализа для всех сценариев этой игры.
- `SystemPrompt` хранит глобальный служебный промт (`analysis_metadata_generator`).
- Команда `seed_demo_content` формирует 1 игру и 3 сценария demo-набора, включая промты и системный промт.

### Что не сделано в этом шаге
- Полная интеграция новых моделей content в runtime-диалог (`DialogSession` пока не ссылается на `game/scenario/scenario_prompt_used`).
- Backoffice-интерфейс продукта (реализовано только через Django Admin).
- Заполнение и валидация реальных медиа-файлов в dev seed-потоке.

## Шаг 4 — Home и маршрутизация пользовательского входа

Дата: 2026-04-04

### Что сделано
- Реализована главная страница после входа (`HomeView` + `templates/pages/home.html`) с выводом:
  - списка игр,
  - кнопок сценариев внутри каждой игры,
  - нижних кнопок «Энциклопедия», «Личный кабинет», «Выйти».
- Добавлен POST-маршрут запуска сценария `scenario_start`.
- Добавлена проверка инварианта «один активный диалог на пользователя» через сервис `has_active_dialog`.
- При отсутствии активного диалога создаётся `DialogSession` и выполняется переход на страницу текущего диалога.
- Добавлены маршруты и временный placeholder экрана диалога.
- Добавлены страницы входа в разделы «Энциклопедия» и «Личный кабинет».
- Обновлён корневой роутер `config/urls.py`.
- Добавлены тесты `apps/core/tests/test_home_flow.py`.

### Как это связано между собой
- `HomeView` берёт опубликованные игры/сценарии из content-моделей и отдаёт их в шаблон.
- Кнопка сценария отправляет POST на `ScenarioStartView`, где выполняется серверная проверка активной сессии.
- Сервис `has_active_dialog` используется и для рендера предупреждения на home, и для блокировки повторного старта.
- Успешный запуск создаёт `DialogSession` и ведёт на `dialogs:placeholder`.

### Что не сделано в этом шаге
- Полноценный чатовый runtime (будет реализован на следующем этапе).
- Полная функциональность энциклопедии и личного кабинета (на этом шаге добавлены входные страницы).

## Шаг 5 — DialogSession lifecycle (создание + чат)

Дата: 2026-04-04

### Что сделано
- Расширена модель `DialogSession`:
  - добавлены связи `game`, `scenario`, `scenario_prompt_used`,
  - добавлены snapshot-поля `conditions_snapshot_text`, `opening_message_snapshot_text`,
  - добавлен флаг `pending_response`.
- Добавлена модель `DialogMessage` с порядком сообщений, ролью, текстом, длиной и `client_message_id` для idempotency.
- Реализовано создание сессии и стартового сообщения персонажа в `ScenarioStartView`.
- Реализован чатовый экран `dialogs/chat.html` согласно композиции UI_base_chat (header, conditions, message list, composer, typing indicator).
- Реализован JSON-endpoint отправки сообщения `dialogs:send_message`.
- Добавлен сервис `send_user_message` с:
  - краткоживущей cache-блокировкой от конкурентных отправок,
  - idempotency-защитой по `client_message_id`,
  - сохранением user/assistant сообщений и обновлением счётчиков сессии.
- Добавлен временный адаптер `generate_game_reply` в integrations.
- Добавлена миграция `apps/dialogs/migrations/0002_dialog_chat_runtime.py`.
- Добавлены тесты для API отправки сообщений `apps/dialogs/tests/test_send_message_api.py` и обновлены тесты home-flow.

### Как это связано между собой
- Home -> `ScenarioStartView` создаёт `DialogSession` и первое `DialogMessage` ассистента.
- Экран `DialogChatView` рендерит историю из `DialogMessage`.
- JS `static/js/chat.js` отправляет сообщения на JSON-endpoint.
- `DialogSendMessageApiView` вызывает сервис `send_user_message`, который централизует все проверки конкурентности/idempotency и сохранение.

### Что не сделано в этом шаге
- Финальная интеграция с внешним LLM (используется временный адаптер-стаб).
- Логика завершения диалога кнопкой «Дай обратную связь» и таймерный auto-finish.
- Расширенные лимиты длины сообщения/ответа через PlatformSettings.

## Шаг 6 — Таймер и завершение сессии (итерация 7)

Дата: 2026-04-04

### Что сделано
- Добавлено фиксируемое поле `effective_duration_seconds` в `DialogSession` и миграция `0003_dialogsession_effective_duration_seconds`.
- На старте сценария `ScenarioStartView` фиксирует эффективную длительность сессии из настроек (`DIALOG_DEFAULT_DURATION_SECONDS`).
- Добавлен lifecycle-сервис завершения `apps/dialogs/services/lifecycle.py`:
  - ручное завершение,
  - завершение по таймеру,
  - завершение по сигналу ухода со страницы,
  - серверный fallback `finalize_stale_page_leave_dialogs`.
- Добавлены endpoint-ы:
  - `dialogs:finish` (manual/timeout),
  - `dialogs:page_leave` (sendBeacon-канал).
- Экран чата получил серверно-синхронизированный таймер и активную кнопку «Дай обратную связь».
- Клиентский `chat.js` обновлён:
  - обратный отсчёт,
  - auto-finish при `00:00`,
  - ручное завершение,
  - `sendBeacon` + keepalive fallback при `pagehide`.
- Добавлены тесты `apps/dialogs/tests/test_finish_api.py`.

### Как это связано между собой
- `DialogChatView` передаёт `seconds_remaining` и блокирует отправку после автозавершения.
- `DialogFinishApiView` использует единый сервис `finish_dialog`, поэтому завершение идемпотентно и консистентно.
- `DialogPageLeaveApiView` принимает браузерный сигнал при закрытии вкладки и фиксирует `page_leave`.
- `has_active_dialog` запускает fallback-добивание «зависших» сессий, чтобы не блокировать запуск новых сценариев.

### Что не сделано в этом шаге
- Переход на страницу анализа после завершения (будет реализован в модуле анализа/результатов).
- Периодический серверный cron/worker для добивания вне HTTP-трафика.

## Шаг 7 — Analytics engine (итерация 8)

Дата: 2026-04-04

### Что сделано
- Расширен домен анализа моделью `AnalysisResult` со snapshot-полями критерия, статусом валидации JSON и raw/parsed данными ответа LLM.
- Добавлен домен `auditlog` с моделью `AuditLogEntry` для технического логирования событий анализа и ошибок.
- Реализован `AnalysisService` (`run_analysis_for_dialog`) с полным циклом:
  - сбор транскрипта,
  - запуск по каждому активному `AnalysisPrompt` игры,
  - валидация/нормализация JSON (`parse_analysis_response`),
  - сохранение `AnalysisRun` + `AnalysisResult`,
  - запись событий в audit log.
- Добавлена обработка невалидных ответов: при `invalid_json/invalid_schema` сохраняется fallback-результат со статусом `fallback_saved` и warning-событием `analysis.invalid_json`.
- В `finish_dialog` встроен запуск аналитики после завершения сессии, если есть хотя бы одна пользовательская реплика.
- Добавлен временный LLM-адаптер `generate_analysis_reply`.
- Добавлены миграции:
  - `apps/analysis/migrations/0002_analysisresult.py`,
  - `apps/auditlog/migrations/0001_initial.py`.
- Добавлены тесты `apps/analysis/tests/test_engine.py`.

### Что не сделано в этом шаге
- Отображение результатов анализа на отдельной пользовательской странице.
- Подключение реального внешнего LLM для аналитики (используется адаптер-заглушка).

## Шаг 8 — Экран результатов и защита доступа (итерация 9)

Дата: 2026-04-04

### Что сделано
- Реализован HTML-экран результатов `GET /dialogs/<dialog_public_id>/results/`.
- Добавлен сервис `build_dialog_results_view_model` для расчёта суммы `N из M` и подготовки карточек по критериям.
- Добавлено представление `DialogResultsView` с защитой доступа: пользователь видит только свои результаты.
- Для активного диалога выполнен безопасный redirect обратно в чат.
- Добавлен шаблон `templates/dialogs/results.html` с:
  - суммой `N из M`,
  - карточками критериев,
  - статусом завершения,
  - контролируемым empty state,
  - warning при частичных проблемах анализа.
- В чат-конфиг добавлен `resultsUrl`, после успешного завершения диалога фронтенд перенаправляет на экран результатов.
- Добавлены тесты `apps/analysis/tests/test_results_view.py` (доступ владельца и запрет доступа к чужому результату).

### Что не сделано в этом шаге
- Экспорт результатов в PDF.
- Полноценный отдельный раздел истории результатов в личном кабинете.

## Шаг 9 — PDF-экспорт результата (итерация 10)

Дата: 2026-04-04

### Что сделано
- Реализован endpoint `GET /dialogs/<dialog_public_id>/export-pdf/`.
- Добавлено представление `DialogExportPdfView` с owner-only доступом к экспорту.
- Реализован сервис `build_result_pdf` на базе `reportlab`:
  - шрифты и размеры по `PDF_LAYOUT_REQUIREMENTS`;
  - цветовые акценты `#004cde`;
  - постраничная нумерация в формате `n из A` внизу справа (`#A7A7A7`, 8pt);
  - блок метаданных, сумма `N из M`, карточки критериев, транскрипт диалога.
- Добавлена кнопка «Экспорт результатов в PDF» на странице `results.html`.
- Добавлен dependency `reportlab` в `requirements.txt`.
- Добавлены тесты `apps/analysis/tests/test_export_pdf.py`:
  - длинный транскрипт,
  - повторный экспорт,
  - запрет экспорта чужих данных.

### Что не сделано в этом шаге
- Кастомная типографика с embedded Arial TTF (используется стандартный `Helvetica` как системно доступный аналог).

## Шаг 10 — Энциклопедия (итерация 11)

Дата: 2026-04-04

### Что сделано
- Реализована отдельная модель `EncyclopediaArticle`:
  - `title`, `slug`, `body`, `summary`, `is_published`, `created_by`, `updated_by`, `created_at`, `updated_at`.
- Добавлена авто-генерация `slug` при создании статьи с защитой от коллизий.
- Добавлены серверные валидации:
  - запрет HTML-тегов в `title` и `summary`;
  - ограничение длины `summary` от 50 до 255 символов.
- Реализован пользовательский раздел энциклопедии:
  - список `/encyclopedia/` с пагинацией по 10,
  - детальная `/encyclopedia/<slug>/`.
- Список показывает только `is_published=True` статьи.
- Реализована сортировка списка: цифры → кириллица → латиница.
- В админке добавлено управление статьями (`EncyclopediaArticleAdmin`) и LLM-генерация summary:
  - авто-генерация при пустом поле на сохранении,
  - action для массовой перегенерации summary.
- Добавлен LLM-адаптер `generate_encyclopedia_summary`.
- Добавлена миграция `apps/content/migrations/0002_encyclopediaarticle.py`.
- Добавлены тесты `apps/content/tests/test_encyclopedia.py`.

### Что не сделано в этом шаге
- Полнотекстовый поиск по энциклопедии.
- Категории статей (архитектурно предусмотрены, UI не реализован).

## Шаг 11 — Личный кабинет и персональный таймер (итерация 12)

Дата: 2026-04-04

### Что сделано
- Реализован полноценный экран личного кабинета `/cabinet/`:
  - профиль (nickname editable, email read-only),
  - переход в password reset flow,
  - блок «Мои показатели»,
  - сильные/западающие индикаторы,
  - история завершённых игр с анализом,
  - ссылки на результат и PDF,
  - блок персонального таймера.
- Добавлен сервис агрегации данных кабинета `build_cabinet_dashboard`.
- История игр ограничена завершёнными сессиями с выполненным анализом и имеет пагинацию по 10 записей.
- Добавлен персональный таймер пользователя `preferred_dialog_duration_minutes` (5–20 минут).
- Добавлена форма `PersonalTimerForm` с серверной валидацией диапазона 5..20.
- На старте сценария теперь используется персональный таймер пользователя для новых сессий.
- Добавлены тесты:
  - `apps/accounts/tests/test_cabinet.py`,
  - обновлён `apps/core/tests/test_home_flow.py` (проверка применения персонального таймера).
- Добавлена миграция `apps/accounts/migrations/0002_user_preferred_dialog_duration_minutes.py`.

### Что не сделано в этом шаге
- Графики и расширенные фильтры истории.
- Отдельный API-контур кабинета для мобильного клиента (будет добавлен позже).

## Шаг 13 — UI/UX доработки, PDF-кириллица и уточнение сценарных полей

Дата: 2026-04-05

### Что сделано
- Исправлена проблема «квадратиков» в PDF: добавлена регистрация кириллических TTF-шрифтов DejaVu и использование их в стилях экспорта.
- Убраны всплывающие цветные уведомления о процессах:
  - отключён рендер `messages` в базовом layout;
  - JS `alert` на чатовой странице заменены на нейтральную строку статуса в интерфейсе.
- На экране результатов:
  - удалена строка «Статус завершения...»;
  - добавлены кнопки «Обратно к упражнениям» сверху и снизу.
- В верхней навигации сайта «Профиль» заменён на «Личный кабинет».
- Формы входа/регистрации/восстановления визуально выровнены по центру; кнопки переведены в full-width стиль.
- Входной экран:
  - «Регистрация» оформлена как оранжевая кнопка;
  - «Забыли пароль?» оформлена как серая кнопка.
- Обновлены цвета кнопок проекта:
  - primary: `#004cde`;
  - warning: `#ff5400`.
- В `ScenarioAdmin` добавлены help-text подсказки для различения полей «Краткое описание» и «Условия сценария».

### Что не сделано в этом шаге
- Не изменялась бизнес-логика ролей и доступа.
- Не выполнялись новые миграции.

## Шаг 14 — LLM в чате без заглушек + видимость энциклопедии для пользователя

Дата: 2026-04-05

### Что сделано
- В `apps/integrations/services/llm_chat.py` реализован реальный OpenAI-compatible HTTP-клиент `/chat/completions`.
- В runtime-конфиг подключены `.env` параметры `LLM_API_KEY`, `LLM_BASE_URL`, `AI_MODEL_NAME` (с timeout).
- В `config/settings/base.py` добавлена загрузка `.env` без внешних библиотек.
- В `local/production` settings добавлено чтение новых LLM-переменных с fallback на legacy `AI_*`.
- Для chat API добавлен возврат `llm_status_text`; на фронте строка статуса добавляется в диалог перед репликой ассистента.
- Заглушка игрового ответа удалена: генерация идёт только через LLM-вызов.
- При ошибке подключения к LLM backend отдаёт контролируемую ошибку уровня API.
- Пользовательская энциклопедия теперь показывает статьи из админки без фильтра по `is_published`.
- Обновлены тесты dialog/encyclopedia под новую фактическую логику.

### Что не сделано в этом шаге
- Не добавлялись миграции.
- Не менялась PDF-логика.
